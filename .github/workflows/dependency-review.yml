name: Dependency Review

on:
  pull_request:
    branches: [main]

jobs:
  dependency-review:
    uses: ospo-sandbox/ospo-test/.github/workflows/dependency-review-reusable.yml@main
    permissions:
      contents: read
